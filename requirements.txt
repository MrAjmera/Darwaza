pydantic==2.9.2
pytest==8.3.3
cryptography==43.0.3
portalocker==4.3.0
fastapi==0.115.5
uvicorn==0.32.1
httpx==0.27.2

# Optional — only needed if you configure the corresponding env vars.
# The test suite and the CLI's fallback paths run without either of
# these; both are imported lazily (inside a function, not at module
# load) exactly so that's true. See DECISIONS.md #6/#7.
# anthropic==0.39.0     # for llm_explainer's live path (ANTHROPIC_API_KEY) and buyer_agent.decide_with_llm()
# razorpay==1.4.2       # for razorpay_client.create_order() (RAZORPAY_KEY_ID/RAZORPAY_KEY_SECRET)
